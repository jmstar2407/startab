{
  "rules": {
    "startab": {
      "v2": {
        "users": {
          "$uid": {
            ".read": "auth != null && auth.uid === $uid",
            ".write": "auth != null && auth.uid === $uid",
            "devices": {
              "$deviceId": {
                "commands": {
                  "$id": {
                    ".validate": "newData.hasChildren(['id','clientAt','expiresAtClient','payload']) && newData.child('id').isString() && newData.child('id').val() === $id && newData.child('clientAt').isNumber() && newData.child('expiresAtClient').isNumber() && newData.child('expiresAtClient').val() > now && newData.child('expiresAtClient').val() <= now + 30000"
                  }
                }
              }
            }
          }
        }
      },
      "pairing": {"tv": {"$session": {".read": "data.exists()", ".write": true}}}
    }
  }
}
